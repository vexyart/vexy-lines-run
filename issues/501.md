Improve vexy-lines-run GUI: 

1. [ ] In the Images mode, when the list is empty, it should say "Drop images here". Change the "+" button underneath the list to "Add Images…". 

2. [ ] In the Lines mode, when the list is empty, it should say "Drop lines here". Change the "+" button underneath the list to say "Add Lines…"

3. [ ] When the Style/End Style is empty, the preview area should say "Drop lines here". Change the "+" button at the bottom of that section to say "Open Lines…"

4. [ ] In the Video mode, change the "+" button to say "Open Video…" and make the "Video" placeholder for label blank. 

5. [ ] Analyze vexy-lines-utils/_private/gui/ctkdemo.py and into our vexy-lines-run add tooltips everywhere 

6. [ ] The bottom area should consist of, in one line: 

- [x] "Export as" label (exists)
- [x] "Format" dropdown (exists)
- [x] "Size" dropdown (exists)
- [x] "Audio" toggle (exists)
- [ ] TODO https://customtkinter.tomschimansky.com/documentation/widgets/progressbar that takes the full available width but is HIDDEN by default.  
- [x] "Export" button (exists)

7. [ ] Change the color of the "Export" button to green (not red). 

8. [ ] When I start the export, the green "Export" button should be replaced by a red "Stop ■" button. If user clicks it, we abort the running export. 

9. [ ] To the Export menu add a "Stop" entry that will abort the running export.  

10. [ ] When we drop a .lines to Style/End Style or when we choose an image in Images list or a lines in Lines list, or when we open a video, the image preview widgets should take the appropriate image’s aspect ratio and should fill the available space without causing overflows(scrolls). 

11. [ ] When I resize the app window, the image preview widgets should resize to again fill the available space. 


